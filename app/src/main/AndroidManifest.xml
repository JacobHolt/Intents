<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jsu.lab4a">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tipCalculatorActivty">
        </activity>
        <activity
            android:name=".milesToKilometersActivity"
            android:label="@string/title_activity_miles_to_kilometers"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".fahrenheitToCelsiusActivity"
            android:label="@string/title_activity_fahrenheit_to_celsius"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>